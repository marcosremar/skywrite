%*******************************************************
% Custom Theme Strings
% Loads translated strings from metadata.yaml
%*******************************************************

% This file is auto-generated during build to set theme strings
% based on the language specified in metadata.yaml

% The strings are set by the build system reading metadata['strings']
% and generating LaTeX commands here

% Example usage in metadata.yaml:
% strings:
%   contents: "Sumário"        (PT) or "Sommaire" (FR)
%   listfigures: "Lista de Figuras" (PT) or "Liste des figures" (FR)
%   etc.

% These will be set dynamically during build process
